<div class="w-10 flex flex-col items-start">
    <%= button_to upvote_submission_comment_path(submission,comment), method: :put, data: { turbo: false }, class:"block text-center group" do %>
        <%= render_svg "icons/up-vote", styles:"fill-current text-gray-500 w-4 h-4 group-hover:text-indigo-600" %>
    <% end %>
    <div class="flex justify-center pl-1">
        <span class="text-gray-700 text-center block" id="<%= dom_id(comment) %>_vote_count"><%= comment.total_vote_count %></span>
    </div>

    <%= button_to downvote_submission_comment_path(submission,comment), method: :put, data: { turbo: false }, class:"block text-center group" do %>
        <%= render_svg "icons/down-vote", styles:"fill-current text-gray-500 w-4 h-4 group-hover:text-indigo-600" %>
    <% end %>
</div>