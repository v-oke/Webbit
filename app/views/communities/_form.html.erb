<%= form_with(model: community) do |form| %>
  <% if community.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(community.errors.count, "error") %> prohibited this community from being saved:</h2>

      <ul>
        <% community.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-6">
    <%= form.label :name  %>
    <%= form.text_field :name, class:"input" %>
  </div>

  <div class="mb-6">
    <%= form.label :title  %>
    <%= form.text_field :title, class:"input" %>
  </div>

  <div class="mb-6">
    <%= form.label :description %>
    <%= form.textarea :description, class:"input min-h-[140px]" %>
  </div>

  <div class="mb-6">
    <%= form.label :sidebar  %>
    <%= form.textarea :sidebar, class:"input min-h-[140px]" %>
  </div>
  
  <div>
    <%= form.submit class:"btn btn-indigo" %>
  </div>
<% end %>
