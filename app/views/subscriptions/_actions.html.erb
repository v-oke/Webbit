<% if user.communities.include? community %>
    <%= button_to "Unsubscribe", community_subscription_path(community,subscription), method: :delete, data: { turbo: false }, class: "btn btn-white w-full" %>
<% else %>
    <%= button_to "Subscribe", community_subscriptions_path(community), method: :post, class: "btn btn-indigo w-full    " %>
<% end %>